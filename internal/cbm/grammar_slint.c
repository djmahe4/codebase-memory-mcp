// Vendored tree-sitter grammar: slint
// Each grammar compiled as separate unit (conflicting static symbols).
#include "vendored/grammars/slint/parser.c"
#include "vendored/grammars/slint/scanner.c"
